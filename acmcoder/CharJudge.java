package acmcoder;

/**
 * 2018/6/12 16:03
 *   判断字符串b的所有字符是否都在字符串a中出现过，a、b都是可能包含汉字的字符串。
 *   b中重复出现的汉字，那么a中也要至少重复相同的次数。
 *   汉字使用gbk编码（简单的说，用两个字节表示一个汉字，高字节最高位为1的代表汉字，低字节最高位可以不为1）。
 *   int is_include(char *a, char *b);
 *   返回0表示没有都出现过，返回1表示都出现过。
 *   请设计一个算法。
 *   样例输入
 *   aaaabbbcccdddss
 *   abc
 *   样例输出
 *   1
 */
public class CharJudge {
    public static void main(String[] args) {

    }

}
